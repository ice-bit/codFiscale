export interface Identity {
    name: string,
    surname: string,
    sex: string,
    birthPlace: string,
    pr: string,
    birthDay: number,
    birthMonth: number,
    birthYear: string,
    codFiscale: string
};